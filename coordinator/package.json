{
  "name": "mesh-gpu-coordinator",
  "version": "0.2.0",
  "private": true,
  "description": "MeshGPU coordinator — LAN discovery, worker pool, and an OpenAI-compatible endpoint.",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "bonjour-service": "^1.3.0",
    "ws": "^8.18.0"
  }
}
